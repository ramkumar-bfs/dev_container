services:
  app:
    init: true # Use init to reap zombies and handle signals
    stop_signal: SIGINT # Ensure bash receives SIGINT on stop
    stop_grace_period: 5s # Give shell time to exit cleanly
    tty: true # Keep an interactive TTY attached
    stdin_open: true # Keep STDIN open for interactive shells
